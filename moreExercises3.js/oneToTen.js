function solve(a, b, c, d, e, f, g, h, i) {
    a = +a;
    b = +b;
    c = +c;
    d = +d;
    e = +e;
    f = +f;
    g = +g;
    h = +h;
    i = +i;
    if (a > 0){
        console.log(`Result: ${(a * 2).toFixed(2)}`);
    } if (b > 0){
        console.log(`Result: ${(b * 2).toFixed(2)}`);
    } if (c > 0){
        console.log(`Result: ${(c * 2).toFixed(2)}`);
    } if (d > 0){
        console.log(`Result: ${(d * 2).toFixed(2)}`);
    } if (e > 0){
        console.log(`Result: ${(e * 2).toFixed(2)}`);
    } if (f > 0){
        console.log(`Result: ${(f * 2).toFixed(2)}`);
    } if (g > 0){
        console.log(`Result: ${(g * 2).toFixed(2)}`);
    } if (h > 0){
        console.log(`Result: ${(h * 2).toFixed(2)}`);
    } if (i > 0){
        console.log(`Result: ${(i * 2).toFixed(2)}`);
    } 
    
}
solve(12, 43, 12, 543, -20);