function project(name, projects) {
    let totalProjHours = projects * 3;
    console.log(`The architect ${name}  will need ${totalProjHours} hours to complete ${projects} project/s.`);

}
project("George", 4)
    